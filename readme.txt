1. install python and pip
2. pip install mouse
3. pip install pygetwindow
4. double click KeepRemoteActivate.bat
* this job will maximize window "iconnectts2.auo.com", rightclick on the screen, then minimize the window
every 600 sec to keep connection alive 